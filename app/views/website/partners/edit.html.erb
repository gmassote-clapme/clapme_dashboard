<div class="page-header">
  <%= link_to website_partners_path, :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to website_partner_path(@clapme_partner.id), :class => 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>Editing partner</h1>
</div>

<%= render 'form', specific_url: website_partner_path(@clapme_partner.id) %>
